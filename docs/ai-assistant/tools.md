---
sidebar_position: 3
---

# Tools Specification

Спецификация инструментов (tools), поддерживаемых в CodeLab IDE. Инструменты выполняются локально в IDE или в Agent Runtime и позволяют AI ассистенту взаимодействовать с файловой системой, выполнять команды и переключаться между агентами.

## Обзор

Инструменты делятся на три категории:
- **Local Tools** - Выполняются в Agent Runtime (echo, calculator, switch_mode)
- **IDE-side Tools** - Выполняются в IDE через WebSocket (файловые операции, команды)
- **Planned Tools** - Запланированы к реализации (Git операции, UI взаимодействие)

## ✅ Реализованные инструменты

### Local Tools (Agent Runtime)

Эти инструменты выполняются непосредственно в Agent Runtime Service.

#### echo

Эхо-инструмент для тестирования.

**Request:**
```json
{
  "type": "tool_call",
  "tool_name": "echo",
  "call_id": "call_000",
  "args": {
    "text": "Hello, World!"
  }
}
```

**Response:**
```json
{
  "type": "tool_result",
  "call_id": "call_000",
  "result": "Hello, World!"
}
```

**Параметры:**
- `text` (string, обязательно): Текст для эха

---

#### calculator

Вычисление математических выражений.

**Request:**
```json
{
  "type": "tool_call",
  "tool_name": "calculator",
  "call_id": "call_001",
  "args": {
    "expr": "2 + 2 * 2"
  }
}
```

**Response:**
```json
{
  "type": "tool_result",
  "call_id": "call_001",
  "result": "6"
}
```

**Параметры:**
- `expr` (string, обязательно): Математическое выражение

:::warning
Использует `eval()` - не безопасно для production с недоверенным вводом
:::

---

#### switch_mode

Переключение между агентами мультиагентной системы.

**Request:**
```json
{
  "type": "tool_call",
  "tool_name": "switch_mode",
  "call_id": "call_002",
  "args": {
    "mode": "coder",
    "reason": "Need to modify code"
  }
}
```

**Response:**
```json
{
  "type": "tool_result",
  "call_id": "call_002",
  "result": "__SWITCH_MODE__|coder|Need to modify code"
}
```

**Параметры:**
- `mode` (string, обязательно): Целевой агент
  - `orchestrator` - координатор задач
  - `coder` - разработчик кода
  - `architect` - проектировщик
  - `debug` - отладчик
  - `ask` - консультант
- `reason` (string, опционально): Причина переключения

**Подробнее**: [Мультиагентная система](/docs/api/multi-agent-system)

---

### IDE-side Tools

Эти инструменты выполняются в IDE через WebSocket.

#### read_file

Читает содержимое файла по указанному пути. Поддерживает частичное чтение по номерам строк.

**Request:**
```json
{
  "type": "tool_call",
  "tool_name": "read_file",
  "call_id": "call_003",
  "args": {
    "path": "src/auth.js"
  }
}
```

**Request (частичное чтение):**
```json
{
  "type": "tool_call",
  "tool_name": "read_file",
  "call_id": "call_004",
  "args": {
    "path": "src/auth.js",
    "start_line": 10,
    "end_line": 20
  }
}
```

**Response (Success):**
```json
{
  "type": "tool_result",
  "call_id": "call_003",
  "result": {
    "content": "// File content here...",
    "encoding": "utf-8"
  }
}
```

**Response (Error):**
```json
{
  "type": "tool_result",
  "call_id": "call_003",
  "error": {
    "code": "FILE_NOT_FOUND",
    "message": "File not found: src/auth.js"
  }
}
```

**Параметры:**
- `path` (string, обязательно): Относительный путь к файлу
- `encoding` (string, опционально): Кодировка файла (по умолчанию: utf-8)
- `start_line` (integer, опционально): Начальная строка (1-based, inclusive)
- `end_line` (integer, опционально): Конечная строка (1-based, inclusive)

**Ограничения:**
- Максимальный размер файла: 1 MB
- Только UTF-8 кодировка
- Только относительные пути внутри workspace

---

#### write_file

Записывает содержимое в файл (создает, если не существует). Поддерживает автоматическое создание директорий и backup.

**Request:**
```json
{
  "type": "tool_call",
  "tool_name": "write_file",
  "call_id": "call_005",
  "args": {
    "path": "src/logger.js",
    "content": "export const log = (msg) => console.log(msg);",
    "create_dirs": true,
    "backup": true
  }
}
```

**Response:**
```json
{
  "type": "tool_result",
  "call_id": "call_005",
  "result": {
    "success": true,
    "bytes_written": 48
  }
}
```

**Параметры:**
- `path` (string, обязательно): Путь к файлу
- `content` (string, обязательно): Содержимое файла
- `encoding` (string, опционально): Кодировка (по умолчанию: utf-8)
- `create_dirs` (boolean, опционально): Создать родительские директории (по умолчанию: false)
- `backup` (boolean, опционально): Создать backup перед перезаписью (по умолчанию: true)

**Ограничения:**
- Максимальный размер: 1 MB
- Перезаписывает существующий файл

:::danger Опасная операция
`write_file` требует подтверждения пользователя (HITL) при `requires_approval: true`
:::

---

#### list_files

Получение списка файлов и директорий. Поддерживает рекурсивный обход и glob patterns.

**Request:**
```json
{
  "type": "tool_call",
  "tool_name": "list_files",
  "call_id": "call_006",
  "args": {
    "path": "src",
    "recursive": true,
    "pattern": "*.dart"
  }
}
```

**Response:**
```json
{
  "type": "tool_result",
  "call_id": "call_006",
  "result": {
    "files": [
      {"path": "src/main.dart", "type": "file", "size": 1024},
      {"path": "src/auth.dart", "type": "file", "size": 2048}
    ]
  }
}
```

**Параметры:**
- `path` (string, обязательно): Относительный путь к директории (`.` для корня workspace)
- `recursive` (boolean, опционально): Рекурсивный обход (по умолчанию: false)
- `include_hidden` (boolean, опционально): Включить скрытые файлы (по умолчанию: false)
- `pattern` (string, опционально): Glob pattern для фильтрации (например, `*.dart`, `**/*.yaml`)

---

#### create_directory

Создание новой директории.

**Request:**
```json
{
  "type": "tool_call",
  "tool_name": "create_directory",
  "call_id": "call_007",
  "args": {
    "path": "src/utils",
    "recursive": true
  }
}
```

**Response:**
```json
{
  "type": "tool_result",
  "call_id": "call_007",
  "result": {
    "success": true,
    "path": "src/utils"
  }
}
```

**Параметры:**
- `path` (string, обязательно): Относительный путь для новой директории
- `recursive` (boolean, опционально): Создать родительские директории (по умолчанию: true)

---

#### execute_command

Выполнение shell команд в workspace. Опасные команды требуют подтверждения пользователя.

**Request:**
```json
{
  "type": "tool_call",
  "tool_name": "execute_command",
  "call_id": "call_008",
  "args": {
    "command": "flutter pub get",
    "cwd": ".",
    "timeout": 60
  }
}
```

**Response:**
```json
{
  "type": "tool_result",
  "call_id": "call_008",
  "result": {
    "stdout": "Running \"flutter pub get\" in project...\nGot dependencies!",
    "stderr": "",
    "exit_code": 0
  }
}
```

**Параметры:**
- `command` (string, обязательно): Команда для выполнения
- `cwd` (string, опционально): Рабочая директория относительно workspace root
- `timeout` (integer, опционально): Таймаут в секундах (по умолчанию: 60, макс: 300)
- `shell` (boolean, опционально): Запустить через shell (по умолчанию: false)

**Опасные команды (требуют HITL):**
- `rm`, `del`, `rmdir` - удаление файлов
- `dd`, `format` - форматирование дисков
- `sudo`, `su` - повышение привилегий
- `chmod`, `chown` - изменение прав
- `curl`, `wget` с записью в файл

:::danger Очень опасная операция
`execute_command` требует подтверждения пользователя для опасных команд
:::

---

#### search_in_code

Поиск текстовых паттернов в файлах кода с использованием grep.

**Request:**
```json
{
  "type": "tool_call",
  "tool_name": "search_in_code",
  "call_id": "call_009",
  "args": {
    "query": "authenticate",
    "path": "src",
    "file_pattern": "*.dart",
    "case_sensitive": false,
    "max_results": 100
  }
}
```

**Response:**
```json
{
  "type": "tool_result",
  "call_id": "call_009",
  "result": {
    "matches": [
      {
        "file": "src/auth.dart",
        "line": 15,
        "content": "  Future<User> authenticate(String username, String password) async {"
      },
      {
        "file": "src/auth.dart",
        "line": 42,
        "content": "    // Authenticate user with backend"
      }
    ],
    "total": 2
  }
}
```

**Параметры:**
- `query` (string, обязательно): Поисковый запрос (текст или regex)
- `path` (string, опционально): Директория для поиска (по умолчанию: `.`)
- `file_pattern` (string, опционально): Glob pattern для фильтрации файлов
- `case_sensitive` (boolean, опционально): Регистрозависимый поиск (по умолчанию: false)
- `regex` (boolean, опционально): Использовать regex (по умолчанию: false)
- `max_results` (integer, опционально): Максимум результатов (по умолчанию: 100, макс: 1000)

---

## 📋 Запланированные инструменты

Эти инструменты будут добавлены в будущих версиях.

### Git Operations

#### git.diff

Получение git diff для указанного пути.

```json
{
  "tool_name": "git.diff",
  "args": {
    "path": ".",
    "staged": false
  }
}
```

**Статус**: 📋 Планируется в v1.2

---

#### git.status

Получение git status.

```json
{
  "tool_name": "git.status",
  "args": {
    "path": "."
  }
}
```

**Статус**: 📋 Планируется в v1.2

---

#### git.commit

Создание коммита.

```json
{
  "tool_name": "git.commit",
  "args": {
    "message": "Add authentication logging"
  }
}
```

**Статус**: 📋 Планируется в v1.2

---

#### git.log

История коммитов.

```json
{
  "tool_name": "git.log",
  "args": {
    "limit": 10,
    "path": "src/auth.js"
  }
}
```

**Статус**: 📋 Планируется в v2.0

---

#### git.branch

Управление ветками.

```json
{
  "tool_name": "git.branch",
  "args": {
    "action": "create",
    "name": "feature/new-auth"
  }
}
```

**Статус**: 📋 Планируется в v2.0

---

### User Interaction

#### apply_patch

Применение diff/patch к рабочей директории.

```json
{
  "tool_name": "apply_patch",
  "args": {
    "diff": "diff --git a/src/auth.js..."
  }
}
```

**Статус**: 📋 Планируется в v1.1

---

#### apply_patch_review

Показывает Patch Review UI для интерактивного выбора изменений.

```json
{
  "tool_name": "apply_patch_review",
  "args": {
    "diff": "diff --git...",
    "message": "Review changes to authentication module"
  }
}
```

**Статус**: 📋 Планируется в v1.1

---

#### prompt_user

Запрос подтверждения или выбора действия у пользователя.

```json
{
  "tool_name": "prompt_user",
  "args": {
    "message": "The changes will modify 5 files. Do you want to continue?",
    "actions": ["approve", "deny", "review"]
  }
}
```

**Статус**: 📋 Планируется в v1.1

---

### Advanced File Operations

#### delete_file

Удаление файла/директории.

```json
{
  "tool_name": "delete_file",
  "args": {
    "path": "src/old_file.js"
  }
}
```

**Статус**: 📋 Планируется в v1.2

---

#### move_file

Перемещение/переименование файла.

```json
{
  "tool_name": "move_file",
  "args": {
    "from": "src/old.js",
    "to": "src/new.js"
  }
}
```

**Статус**: 📋 Планируется в v1.2

---

### Process Management

#### run_persistent

Запуск долгоживущих процессов.

```json
{
  "tool_name": "run_persistent",
  "args": {
    "command": "npm run dev",
    "process_id": "dev_server"
  }
}
```

**Статус**: 📋 Планируется в v2.0

---

#### kill_process

Остановка запущенного процесса.

```json
{
  "tool_name": "kill_process",
  "args": {
    "process_id": "dev_server"
  }
}
```

**Статус**: 📋 Планируется в v2.0

---

## Сводная таблица инструментов

| Инструмент | Категория | Статус | HITL | Описание |
|-----------|-----------|--------|------|----------|
| `echo` | Local | ✅ Реализован | ❌ | Эхо-инструмент |
| `calculator` | Local | ✅ Реализован | ❌ | Вычисление выражений |
| `switch_mode` | Local | ✅ Реализован | ❌ | Переключение агентов |
| `read_file` | IDE-side | ✅ Реализован | ❌ | Чтение файлов |
| `write_file` | IDE-side | ✅ Реализован | ✅ | Запись файлов |
| `list_files` | IDE-side | ✅ Реализован | ❌ | Список файлов |
| `create_directory` | IDE-side | ✅ Реализован | ❌ | Создание директорий |
| `execute_command` | IDE-side | ✅ Реализован | ✅* | Выполнение команд |
| `search_in_code` | IDE-side | ✅ Реализован | ❌ | Поиск в коде |
| `git.diff` | Git | 📋 Планируется | ❌ | Git diff |
| `git.status` | Git | 📋 Планируется | ❌ | Git status |
| `git.commit` | Git | 📋 Планируется | ✅ | Git commit |
| `git.log` | Git | 📋 Планируется | ❌ | Git log |
| `git.branch` | Git | 📋 Планируется | ❌ | Git branch |
| `apply_patch` | UI | 📋 Планируется | ✅ | Применение patch |
| `apply_patch_review` | UI | 📋 Планируется | ✅ | Review patch |
| `prompt_user` | UI | 📋 Планируется | - | Запрос пользователя |
| `delete_file` | File | 📋 Планируется | ✅ | Удаление файлов |
| `move_file` | File | 📋 Планируется | ✅ | Перемещение файлов |
| `run_persistent` | Process | 📋 Планируется | ✅ | Долгоживущие процессы |
| `kill_process` | Process | 📋 Планируется | ❌ | Остановка процессов |

*✅ = требует HITL для опасных команд

## Ограничения

### Размеры файлов

| Операция | Максимальный размер |
|---|---|
| `read_file` / `write_file` | 1 MB |
| `git.diff` / `apply_patch` | 5 MB (планируется) |
| WebSocket сообщение | 10 MB |
| `user_message` | 10,000 символов |

### Пути файлов

- Только относительные пути внутри workspace
- Максимальная длина пути: 255 символов
- Запрещены пути с `..` (выход за пределы workspace)

### Кодировка

- Только UTF-8
- Бинарные файлы не поддерживаются

## Коды ошибок

| Код | Описание |
|---|---|
| `FILE_NOT_FOUND` | Файл не найден |
| `FILE_TOO_LARGE` | Файл превышает максимальный размер |
| `PERMISSION_DENIED` | Нет прав доступа |
| `INVALID_PATH` | Некорректный путь |
| `PATH_OUTSIDE_WORKSPACE` | Путь за пределами workspace |
| `GIT_NOT_INITIALIZED` | Git репозиторий не инициализирован |
| `GIT_ERROR` | Ошибка выполнения git команды |
| `PATCH_APPLY_FAILED` | Не удалось применить patch |
| `ENCODING_ERROR` | Ошибка кодировки |
| `TOOL_NOT_FOUND` | Инструмент не найден |
| `INVALID_ARGUMENTS` | Некорректные аргументы |
| `COMMAND_TIMEOUT` | Превышен таймаут выполнения команды |
| `COMMAND_FAILED` | Команда завершилась с ошибкой |

## План развития

| Версия | Инструменты | Срок | Статус |
|--------|-------------|------|--------|
| **v1.0 (MVP)** | echo, calculator, switch_mode, read_file, write_file, list_files, create_directory, execute_command, search_in_code | Q1 2026 | ✅ Завершено |
| **v1.1** | apply_patch, apply_patch_review, prompt_user | Q2 2026 | 📋 Планируется |
| **v1.2** | git.status, git.commit, delete_file, move_file | Q2 2026 | 📋 Планируется |
| **v2.0** | git.diff, git.log, git.branch, run_persistent, kill_process | Q3 2026 | 📋 Планируется |

## Примеры использования

### Пример 1: Чтение и модификация файла

```json
// 1. Прочитать файл
{
  "type": "tool_call",
  "tool_name": "read_file",
  "call_id": "call_001",
  "args": {"path": "src/config.js"}
}

// 2. Получить содержимое
{
  "type": "tool_result",
  "call_id": "call_001",
  "result": {"content": "const config = {...}"}
}

// 3. Записать изменения
{
  "type": "tool_call",
  "tool_name": "write_file",
  "call_id": "call_002",
  "args": {
    "path": "src/config.js",
    "content": "const config = {...updated...}",
    "backup": true
  },
  "requires_approval": true
}
```

### Пример 2: Поиск и создание файла

```json
// 1. Поиск существующих файлов
{
  "type": "tool_call",
  "tool_name": "search_in_code",
  "call_id": "call_003",
  "args": {
    "query": "authenticate",
    "file_pattern": "*.dart"
  }
}

// 2. Создание новой директории
{
  "type": "tool_call",
  "tool_name": "create_directory",
  "call_id": "call_004",
  "args": {
    "path": "lib/auth",
    "recursive": true
  }
}

// 3. Создание нового файла
{
  "type": "tool_call",
  "tool_name": "write_file",
  "call_id": "call_005",
  "args": {
    "path": "lib/auth/auth_service.dart",
    "content": "class AuthService {...}",
    "create_dirs": true
  }
}
```

### Пример 3: Выполнение команд

```json
// 1. Установка зависимостей
{
  "type": "tool_call",
  "tool_name": "execute_command",
  "call_id": "call_006",
  "args": {
    "command": "flutter pub get",
    "timeout": 120
  }
}

// 2. Запуск тестов
{
  "type": "tool_call",
  "tool_name": "execute_command",
  "call_id": "call_007",
  "args": {
    "command": "flutter test",
    "cwd": ".",
    "timeout": 300
  }
}
```

## Совместимость

Все компоненты системы используют эту спецификацию:

- ✅ **CodeLab IDE**: Реализует локальное выполнение IDE-side tools
- ✅ **Gateway Service**: Маршрутизирует tool_call и tool_result
- ✅ **Agent Runtime Service**: Генерирует tool_call на основе LLM решений, выполняет local tools
- ✅ **LLM Proxy Service**: Передает информацию о доступных tools в LLM

## Версионирование

- **Версия 1.0** (текущая): 9 реализованных инструментов
- **Версия 1.1**: + UI interaction tools
- **Версия 1.2**: + Git operations
- **Версия 2.0**: + Process management

Все изменения в спецификации должны быть обратно совместимыми или явно указывать breaking changes.

## Дополнительные ресурсы

- [WebSocket Protocol](/docs/api/websocket-protocol) - Протокол взаимодействия
- [Agent Protocol](/docs/api/agent-protocol) - Расширенный протокол агента
- [Мультиагентная система](/docs/api/multi-agent-system) - Документация по агентам
- [Архитектура](/docs/architecture/overview) - Обзор архитектуры
- [GitHub Repository](https://github.com/pese-git/codelab-workspace)
