---
sidebar_position: 5
title: Создание первого проекта
description: Пошаговое руководство по созданию первого проекта в CodeLab IDE
---

# Создание первого проекта

Это пошаговое руководство поможет вам создать ваш первый проект в CodeLab IDE и познакомиться с основными возможностями AI ассистента.

## 📋 Что вы узнаете

В этом руководстве вы:
- Создадите новый Flutter проект
- Познакомитесь с интерфейсом IDE
- Научитесь работать с AI ассистентом
- Выполните первую задачу с помощью агентов
- Узнаете о HITL (Human-in-the-Loop) механизме

## 🎯 Предварительные требования

Убедитесь, что вы:
- ✅ Установили CodeLab IDE ([инструкция](installation.md))
- ✅ Настроили подключение к AI Service
- ✅ Имеете базовые знания Flutter/Dart (опционально)

## Шаг 1: Запуск CodeLab IDE

### Запуск приложения

**Windows:**
```bash
# Перейдите в директорию с приложением
cd codelab_ide/apps/codelab_ide/build/windows/x64/runner/Release

# Запустите IDE
./codelab_ide.exe
```

**macOS:**
```bash
# Запустите приложение
open codelab_ide/apps/codelab_ide/build/macos/Build/Products/Release/codelab_ide.app
```

**Linux:**
```bash
# Перейдите в директорию с приложением
cd codelab_ide/apps/codelab_ide/build/linux/x64/release/bundle

# Запустите IDE
./codelab_ide
```

### Приветственный экран

При первом запуске вы увидите приветственный экран с тремя опциями:
- **Открыть проект** - открыть существующий проект
- **Создать проект** - создать новый проект
- **Клонировать из Git** - клонировать репозиторий

## Шаг 2: Создание нового проекта

### Выбор типа проекта

1. Нажмите кнопку **"Создать проект"**
2. Выберите тип проекта: **Flutter Application**
3. Укажите имя проекта: `my_first_app`
4. Выберите директорию для проекта
5. Нажмите **"Создать"**

IDE автоматически:
- Создаст структуру Flutter проекта
- Инициализирует Git репозиторий
- Откроет проект в редакторе

### Структура проекта

После создания вы увидите стандартную структуру Flutter проекта:

```
my_first_app/
├── lib/
│   └── main.dart          # Главный файл приложения
├── test/
│   └── widget_test.dart   # Тесты
├── pubspec.yaml           # Зависимости проекта
├── analysis_options.yaml  # Настройки анализатора
└── README.md              # Описание проекта
```

## Шаг 3: Знакомство с интерфейсом

### Основные панели

CodeLab IDE состоит из нескольких панелей:

```
┌─────────────────────────────────────────────────────┐
│  Меню                                               │
├──────────┬──────────────────────────┬───────────────┤
│          │                          │               │
│  Файлы   │   Редактор кода          │  AI Ассистент │
│          │                          │               │
│          │                          │               │
├──────────┴──────────────────────────┴───────────────┤
│  Терминал / Вывод                                   │
└─────────────────────────────────────────────────────┘
```

**Левая панель** - Файловый менеджер
- Навигация по файлам проекта
- Создание/удаление файлов и папок
- Контекстное меню

**Центральная панель** - Редактор кода
- Подсветка синтаксиса
- Нумерация строк
- Автоматическое форматирование

**Правая панель** - AI Ассистент
- Чат с агентами
- История сообщений
- Выбор режима агента

**Нижняя панель** - Терминал и вывод
- Встроенный терминал
- Вывод компиляции
- Логи приложения

## Шаг 4: Первая задача с AI ассистентом

### Подключение к AI Service

1. Откройте панель AI ассистента (правая сторона)
2. Нажмите на иконку настроек ⚙️
3. Проверьте настройки подключения:
   - **Server URL**: `ws://localhost/api/v1/ws` (или ваш адрес)
   - **Session ID**: автоматически генерируется
4. Нажмите **"Подключиться"**

Статус подключения изменится на 🟢 **Connected**.

### Выбор агента

В верхней части панели AI ассистента выберите режим:
- **🎭 Orchestrator** - автоматический выбор агента (рекомендуется)
- **💻 Coder** - для написания кода
- **🏗️ Architect** - для проектирования
- **🐛 Debug** - для отладки
- **💬 Ask** - для вопросов

Для первой задачи выберите **Orchestrator**.

### Постановка задачи

В поле ввода AI ассистента введите:

```
Создай простой виджет LoginForm с полями для email и пароля, 
и кнопкой "Войти". Используй Material Design.
```

Нажмите **Enter** или кнопку отправки.

### Процесс выполнения

AI ассистент начнет работу:

1. **Анализ задачи** - Orchestrator анализирует запрос
2. **Выбор агента** - Orchestrator передает задачу Coder агенту
3. **Планирование** - Coder создает план выполнения
4. **Выполнение** - Coder использует инструменты для создания кода

Вы увидите streaming ответ в реальном времени:

```
🎭 Orchestrator: Анализирую задачу...
Задача требует создания нового виджета. Передаю Coder агенту.

💻 Coder: Создаю LoginForm виджет...
Планирую создать файл lib/widgets/login_form.dart

🔧 Tool: write_file
Путь: lib/widgets/login_form.dart
```

### HITL подтверждение

Когда агент попытается создать файл, появится диалог подтверждения:

```
┌─────────────────────────────────────────┐
│  Подтверждение действия                 │
├─────────────────────────────────────────┤
│  Агент хочет создать файл:              │
│  lib/widgets/login_form.dart            │
│                                         │
│  Предпросмотр:                          │
│  ┌─────────────────────────────────┐   │
│  │ import 'package:flutter/...     │   │
│  │                                 │   │
│  │ class LoginForm extends ...     │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [Одобрить]  [Отклонить]  [Изменить]   │
└─────────────────────────────────────────┘
```

**Опции:**
- **Одобрить** - разрешить выполнение
- **Отклонить** - отменить действие
- **Изменить** - внести правки перед выполнением

Нажмите **"Одобрить"**.

### Результат

После одобрения:
- Файл `lib/widgets/login_form.dart` будет создан
- Файл автоматически откроется в редакторе
- AI ассистент покажет сообщение о завершении

```dart
// lib/widgets/login_form.dart
import 'package:flutter/material.dart';

class LoginForm extends StatefulWidget {
  const LoginForm({Key? key}) : super(key: key);

  @override
  State<LoginForm> createState() => _LoginFormState();
}

class _LoginFormState extends State<LoginForm> {
  final _formKey = GlobalKey<FormState>();
  final _emailController = TextEditingController();
  final _passwordController = TextEditingController();

  @override
  Widget build(BuildContext context) {
    return Form(
      key: _formKey,
      child: Column(
        children: [
          TextFormField(
            controller: _emailController,
            decoration: const InputDecoration(
              labelText: 'Email',
              prefixIcon: Icon(Icons.email),
            ),
            validator: (value) {
              if (value == null || value.isEmpty) {
                return 'Введите email';
              }
              return null;
            },
          ),
          const SizedBox(height: 16),
          TextFormField(
            controller: _passwordController,
            decoration: const InputDecoration(
              labelText: 'Пароль',
              prefixIcon: Icon(Icons.lock),
            ),
            obscureText: true,
            validator: (value) {
              if (value == null || value.isEmpty) {
                return 'Введите пароль';
              }
              return null;
            },
          ),
          const SizedBox(height: 24),
          ElevatedButton(
            onPressed: () {
              if (_formKey.currentState!.validate()) {
                // Обработка входа
              }
            },
            child: const Text('Войти'),
          ),
        ],
      ),
    );
  }

  @override
  void dispose() {
    _emailController.dispose();
    _passwordController.dispose();
    super.dispose();
  }
}
```

## Шаг 5: Запуск приложения

### Использование терминала

1. Откройте терминал (нижняя панель)
2. Выполните команду:

```bash
flutter run -d macos  # или windows, linux
```

Или попросите AI ассистента:

```
Запусти приложение на macOS
```

AI ассистент выполнит команду через инструмент `execute_command`.

### Просмотр результата

После запуска вы увидите:
- Окно приложения с LoginForm виджетом
- Поля для email и пароля
- Кнопку "Войти"

## Шаг 6: Дополнительные задачи

### Добавление валидации

Попросите AI ассистента улучшить форму:

```
Добавь валидацию email с проверкой формата, 
и требование минимум 6 символов для пароля
```

### Создание тестов

```
Создай unit тесты для LoginForm виджета
```

### Улучшение UI

```
Улучши дизайн формы: добавь карточку с тенью, 
скругленные углы и градиентный фон
```

## 💡 Полезные советы

### Работа с AI ассистентом

1. **Будьте конкретны** - четко формулируйте задачи
2. **Разбивайте задачи** - сложные задачи разбивайте на подзадачи
3. **Используйте контекст** - ссылайтесь на существующие файлы
4. **Проверяйте результат** - всегда проверяйте сгенерированный код

### Горячие клавиши

- `Ctrl/Cmd + S` - сохранить файл
- `Ctrl/Cmd + F` - поиск в файле
- `Ctrl/Cmd + Shift + F` - поиск в проекте
- `Ctrl/Cmd + /` - комментировать строку
- `Ctrl/Cmd + \`` - открыть терминал

### HITL настройки

Вы можете настроить уровень автономности:
- **Строгий** - подтверждение всех действий
- **Средний** - подтверждение опасных действий (по умолчанию)
- **Свободный** - минимум подтверждений

## 🎓 Следующие шаги

Теперь, когда вы создали первый проект, изучите:

1. [**Архитектура AI ассистента**](../ai-assistant/overview.md) - как работает мультиагентная система
2. [**Инструменты агентов**](../ai-assistant/tools.md) - какие инструменты доступны
3. [**HITL механизм**](../ai-assistant/hitl.md) - настройка контроля действий
4. [**Руководства**](../guides/auth-integration.md) - практические примеры

## ❓ Часто задаваемые вопросы

### Агент не отвечает

**Проблема:** AI ассистент не отвечает на запросы

**Решение:**
1. Проверьте подключение к AI Service (статус должен быть 🟢)
2. Проверьте логи в терминале
3. Перезапустите IDE

### Ошибка при создании файла

**Проблема:** Ошибка "Permission denied" при создании файла

**Решение:**
1. Проверьте права доступа к директории проекта
2. Запустите IDE с правами администратора (Windows) или sudo (Linux)

### Медленная работа

**Проблема:** AI ассистент медленно отвечает

**Решение:**
1. Проверьте подключение к интернету
2. Используйте локальный LLM (Ollama) для быстрых ответов
3. Уменьшите размер контекста в настройках

## 📚 Дополнительные ресурсы

- [Примеры проектов](https://github.com/codelab/examples)
- [Видео туториалы](https://youtube.com/codelab)
- [Community форум](https://community.codelab.dev)

---

**Поздравляем!** 🎉 Вы создали свой первый проект в CodeLab IDE и познакомились с AI ассистентом. Продолжайте экспериментировать и изучать возможности платформы!
